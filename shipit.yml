dependencies:
  pre:
    - env

deploy:
  override:
    - bundle exec which ruby
    - bundle exec ruby -v

review:
  checklist: []

rollback:
  override:
    - bundle exec which ruby
    - bundle exec which cap
    - bundle exec cap --version
